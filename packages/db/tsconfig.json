{
    "extends": "@repo/typescript-config/base.json",
    "compilerOptions": {
    "types": ["node"] // <--- Add this line explicitly if it's missing
  },
  "include": ["src", "prisma.config.ts"] // <--- Make sure your config file is included
}
